extends ../layout

block append content
  each photo in photos
    .col-md-3
      figure
        img(src="/photos/thumbnail/" + photo.id + ".png")
        figcaption
          a(href="/users/" + users[photo.id].id) #{users[photo.id].name}

  a(href="/feed?page=" + nextPage) more

